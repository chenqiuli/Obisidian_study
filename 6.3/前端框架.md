- /b/business这是网关，本地开发不要这个，
- 列表请求hook ：devExtreme，
- orval -> openapi？orval太散了，要封装请求拦截器和生成器，在一个目录里面。。，第三方万一更新了，我们就会被变了，要重写掉，及时抽包。
- 根目录的openapi 
- src/api：下放微服务名的接口，生成出来的
- 导出一个约定的配置文件，约定式路由，用于判断是否显示组织架构树 - 资产那套
- pages：system-settings、profile【全局、修改密码、个人中心】、业务问田总
- 项目设置 - 平台保留一份底，业务要再做一份新的扩展自己的业务字段
- login：视图与逻辑分离，为了复用运营平台等


#### QA
- Q：6.2现有问题，openapi生成出来的接口落实到services时带了/b/xxx网关。如果我一个人完成前端和后端，要在本地前端联调本地后端的时候，这时候就本地后端在general模块加了一个新接口，前端是openapi刷不到的。还有在部署的时候，部署私有化，网关/b/business；部署saas，网关又是/b/general。但是用openapi刷出来的代码就是一套，带有/b/general的，要部署私有化就要想办法把/b/general改为/b/business。很麻烦，背景原因导致的吧，一开始做6.2只部署到私有化，因此openapi.ts里面的schemaPath是：https://rentx-dev.ifca.cloud/b/business/swagger/General/swagger.json，这个生成出来的代码可以直接部署到私有化。后来才有saas，saas网关不是/b/business，所以就要想办法把/b/business替换为/b/general，于是才有openapi.ts的apiPrefix。然后同一套代码又要部署到saas和私有化，这时候services/general生成出来的接口已经改为/b/general，部署私有化又有问题了，所以他们又在serverconfig.json5里面的modules配置各个模块，把/b/general又改为/b/business。
- A：实现到本地连接后端 - 不带网关；连接Saas - 带/b/general网关；连接私有化 - 带/b/business网关。
<hr />
- Q：Zustand要能读取到serverconfig.json5的配置。需求背景是：我部署到https://rentx-dev.ifca.cloud，若我同时部署到这个环境有dev和prod，出来的链接是：https://rentx-dev.ifca.cloud/dev、https://rentx-dev.ifca.cloud/prod。这时候这两个环境关于Zustand的localStorage数据就会互串。
- A：
<hr />
- Q：公司只给了一个域名，要部署到多个环境，这时候需要用到虚拟目录。那前端产物为了支持同一份 dist 现在可部署到任意虚拟目录
- A：要把dist产物从绝对路径改为相对路径
<hr />
- Q：多语言设计（系统公共、应用公共-客户名称、模块公用、页面独有）、产品菜单、404页面、点击产品进入菜单有问题-菜单折叠了、公共删除、首页热区放哪里设计、公共表格（width-%、px，不要横向滚动条-变成可选参数，按列权重出现，展示已隐藏列-田总有案例）、搬平台公共页面（谭老板有的搬过来，我搬6.2差异化的页面）
- A：
